function parseCSV -> Integer:
    message("opening file")
    let file = file:read("numbers.csv")
    message("performing computation")
    let res = file.split(",").Integer().reduce(lam(a,b): a + b)
    return res
end

let handler = spawn parseCSV()
handler.recieve(print)

print(await res)
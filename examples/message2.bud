def worker:
    sleep_ms(10)
    send("hi")
    send("okay")
    send("bye")
    // @todo locks if you put sleep 300 here

let child = spawn worker()

while(!child.done() || child.pending()):
    print(child.receive())

print("All done")
{
  "name": "daisylang",
  "version": "0.1.0",
  "description": "DaisyLang compiler with ANTLR parser",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "dev": "npm run clean && tsc --watch",
    "start": "npm run build && node dist/index.js",
    "test": "npm run build && node dist/test.js"
  },
  "keywords": [
    "compiler",
    "antlr",
    "daisylang"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.41",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chevrotain": "^12.0.0"
  }
}

FIX
* add continue keyword
* !! add a waitUntil(condition) -> auto cv's
* double printing should std::setprecision(std::numeric_limits<double>::max_digits10) 
* !! Do shared reads really need to mutexed on read???
    -> think yes, data race
    -> maybe type "restructed" will be like this? Probably no still
* exporting a function called "X" then also declaring it locally "X" -> import has no name abstraction = override = BAD
* !! allow new restricted marking var like:
    restricted bob = 10
    -> allows only scope who declared it to modify it
* warnins show line number
* !! export variable isnt imported correctly - always has libname. before still
* allow export of variable identifier, just exports vairable. e.g. let bob = 100; export bob
* !! passing shared struct to parameter type const struct = cpp error
* highlighter, everything starting with cap is red
* update docs with Byte, why const/shared is recommended for arugment passing, modulo, repeat statement (once cleaned up)
* cleanup repeat expression to: repeat(let i = 0, count)
* add const declration to methods so only certain methods can be called on consts. i.e. .push on a const list should throw fail
* add test @@@FAILS@@@ to test cases that should fail

* add a genlist<type>(size)
* all shared reads should be mutexed inline, i.e:
    lock x;
    bob = x;
    unlock x;
    * this is to prevent read then modify on next line race conditions
* add an await_mine()
    -> needs to also store all created handles locally

* make sure program exit doesnt throw if tries to await timeout on join_all

* type aliasing. type keyword becomes struct. add using keyword or alias 
* maybe handler shouldnt be innately shared? force user to write shared handler = spawn ...
* send, receive, etc should not be pseudo globals in functions, the should be like parent:send, parent:receive
    * maybe improve names for builtins for threads msgPending instead of just pending instead
    * pending sounds like its talking about the thread itself
* shared variables are hoisted WHICH IS OK, but that means naming conflicts
    * in scope, variables need to have cname also
* block comment indentation aghghhhhhhh, try below
* get rid of colon syntax for property accesss
* should be file.delete not remove
* improve templating to allow templated typedefs. type Bob<T,Q> = {List<T> a, List<Q> b}
* allow printing struct
* get rid of $ for shared
* !! Add handler.onFinish(function<handler type>), should execute before the await finishes or after?
* make sure explicit List<List<String>> works
* make sure a thread calling another method normally, that method can still call send and receive()
    -> By implementation, this wont work due to fakeChannel. Maybe should stay like this?
* import types are declared globally -> add a types list to classes for namespaced types
    -> requires decent changes in parser and lexer
BUG paste this:
/*
 * To instead run on the active thread (like a normal function), just call it
 * send() and receive() will be noops
let res = calculator("Mila", ["40",  "50", "105"], household)
print("Mila:", res)
*/

BUG
let rio = "is: " + "fat"
print(rio)


FEATURES:
* maybe allow for cppType("SDL_Event")?
* not sure how safe capturing everything by reference in a lambda is. Shareds? Locals? etc
* add some method to shorten:

    repeat(i, num_workers):
        workers.push(spawn computer(worker_height * i, worker_height))

    repeat(i, num_workers):
        await workers[i]
        print("Worker [${i}] done")

    * like workers.spawnAndAwait

* auto backprop to resolve if int or float so you dont have to type 0.0 on creation 
* make a raytracer
* add a timer clas like timer.start() then timer.finish()
* add "share(variable or seomthing)" to cast to shared (first check if its needed at all or it does implicitly)
* add naming parameters
* add expression to call c++ with template strings (evals expression and calls template strings specially) callCPP("Daisy::util::print", v)
    -> wraps in call appropriately

* can you do something like let Handles<String> handles = functions.map(spawn function), handles.forEach(await)
    -> requires await to be an actual function

* allow comma or semi-c to separate lines in lambda, then multi-lines requires a return statement
* !! add export and import similar to ts (maybe use c++20 modules?)
* function explicit type Function<RETURN, ARG1, ARG2,...> i.e. Function<Int, String, String>
* add try-catch
    -> needs to handle _CancelledException()
* add .sort like in js
* add a binary semaphores that can be used across threads to signal ready between threads, signal:wait(mySignal) and signal:send(mySignal)
* DONT use atomic type instead of sharedptr for primitives, but keep sharedptr in all other types for reference counting across threads
    -> wrapped with cancelled exception
* !! classes
* !! templates params when untyped
* dont think parenthesis are passed to generated - things like binary ops should just wrap codegen in ()
* semi colon insertion is lazy, worst case use clang tidy? or preproc that inserts semic into .bud and walker reads that and just adds semic in generator